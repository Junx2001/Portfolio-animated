services:
  portfolio-animated:
    build:
      context: .
      dockerfile: Dockerfile
    working_dir: /app
    volumes:
      - ./:/app
      - /app/node_modules
    command: sh -c "npm install && npm run dev"
    ports:
      - "4321:4321"
    environment:
      NODE_ENV: development
      PORT: 4321